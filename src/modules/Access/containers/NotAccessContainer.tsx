import React from 'react';
import NotAccessComponent from '../components/NotAccessComponent';

const NotAccessContainer = () => {
  return <NotAccessComponent />;
};

export default NotAccessContainer;
