import React, {FC} from 'react';
import {CText, CView} from '../../../components';

const RekapComponent: FC = () => {
  return (
    <CView>
      <CText>RekapComponent</CText>
    </CView>
  );
};

export default RekapComponent;
